# Hasura Cloud / CLI related envs
HASURA_GRAPHQL_ADMIN_SECRET=your_admin_secret
HASURA_ENDPOINT=https://your-hasura-project.hasura.app
# Optional: if using actions/webhooks
HASURA_ACTIONS_WEBHOOK_BASEURL=https://your-app.example.com
